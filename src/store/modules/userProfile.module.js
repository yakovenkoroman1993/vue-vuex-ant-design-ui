let defaultState = {
    firstName: 'John',
    lastName: 'Doe',
    jobTitle: 'X5 retail group X5 retail group X5 retail',
};

let getters = {};

let mutations = {
};

let actions = {};

export default {
    namespaced: true,
    state: defaultState,
    getters,
    mutations,
    actions,
}